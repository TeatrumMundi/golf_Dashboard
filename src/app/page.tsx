import GridStackLayout from "@/components/config/gridLayout";

export default function HomePage() {
    return (
        <main className="min-h-screen bg-neutral-900">
            <GridStackLayout />
        </main>
    );
}
